[build]
  command = "corepack enable && corepack prepare pnpm@10.33.0 --activate && pnpm install --frozen-lockfile && pnpm --filter @flowers-tg/shared run build && pnpm --filter @flowers-tg/mini-app run build"
  publish = "packages/mini-app/dist"

[build.environment]
  NODE_VERSION = "22"

